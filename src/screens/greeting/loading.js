import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const loading = () => {
  return (
    <View>
      <Text>
        loading screen after selecting one option in delivery or pickup
      </Text>
    </View>
  );
};

export default loading;

const styles = StyleSheet.create({});
